language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jbsulli/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jbsulli/docker-react npm run test -- --coverage
